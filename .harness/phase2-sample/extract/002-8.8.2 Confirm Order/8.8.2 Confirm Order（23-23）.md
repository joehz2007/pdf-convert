Table metadata: `data-table-id=p0023-t02` `data-table-role=parent` `data-child-table-ids=p0023-t02-c01`

| Field | Required | Type | Description |
| --- | --- | --- | --- |
| orderId | Y | string | The id of the order |
| partyDetails | C | Array of objects | The participant details arrays of the order, provide at least one originator AND one beneficiary. This must be provided if it has not been provided during payout creation If this is already provided during payout creation, providing this again will replace the previous entry |
| <s>cryptoMethod</s><br/>cryptoAddressInfo | C | Array of objects | Mandatory if settleCurrency = Crypto This must be provided if it has not been provided during payout creation If this is already provided during payout creation, providing this again will replace the previous entry |
| <s>fiatMethod</s><br/>fiatAccountInfo | C | object | Mandatory if settleCurrency = Fiat This must be provided if it has not been provided during payout creation If this is already provided during payout creation, providing this again will replace the previous entry |

**Params of cryptoAddress (objects of cryptoAddressInfo)**

Table metadata: `data-table-id=p0023-t02-c01` `data-table-role=child` `data-parent-table-id=p0023-t02`

| Field | Required | Type | Description |
| --- | --- | --- | --- |
| address | Y | string | Destination address. NOTE: IF NOT PROVIDED, MUST PROVIDE IN CONFIRM STEP |
